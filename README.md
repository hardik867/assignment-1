# assignment-1
in this code we first balance the dataset.
then select 5 samples by various techniques like undersampling,oversampling.
and then try it on different models 
and at last give the accuracy of each model on different sampling technique which is best for each model.
